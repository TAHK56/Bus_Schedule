Наименования использовал как в чинук базе данных.

                       География
Создал 2 таблицы: Локация(города, села и т.д) и Станции(где находиться).
Можно было бы добавить еще таблицу Страна с ограничением уникальная по имени, но я создал проект для
внутреннего рынка; И зависимости соответсвенно в локации может быть не одна станция.
В станции использовал обычные варчар для хранения адреса так как использует только эта таблица, но если бы
например нам надо было бы эти данные  для юзера, компании и т.д то можно создать было бы таблицу Адрес.

                      Расписание и маршрут
Здесь в Маршруте связь на Станцию(отправка и прибытие) и также время, также выбрал имя маршрута уникальным -
что упростило базу данных (не пришлось создавать еще одну таблицу Поездки, например 42 маршрут идет со стании в 10:00,
а затем 16:00);
Расписание по сути хранит остановки маршрута на станции и время прибытие и отбытия, также сделал что время
хранит Null так как если начало поездки то время прибытия нету и на конечной нету отбытия.
Также можно было бы добавить Таблицу для например что только по четным дням или только летом и т.д
В проекте для упрощения сделал что маршруты каждый день!


                    Автобус
Здесь сделал номер автобуса уникальным, можно было бы сделать таблицу Сидения(выбрать например номер места,
класс и  т.д)

                    Пользователь
 Упрощеная таблица которая хранит и покупателя,  админа, оператора(Так как в проекте касса то сделал поля
обязательными только фамилия и имя, но если б надо действительно покупатель то сделал бы пароль и почту
уникальным)

                   Билет
Время продажи установил по умолчанию текущее время, станции откуда и куда, цена формируеться.

